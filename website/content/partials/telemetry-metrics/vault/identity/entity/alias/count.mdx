### vault.identity.entity.alias.count {#vault-identity-entity-alias-count}

Metric type | Value   | Description
----------- | ------- | -----------
gauge       | aliases | The number of identity entities aliases (per authN mount) currently stored in OpenBao

OpenBao updates the alias count every `usage_guage_period` interval.
